import React from 'react';

const AuthBackground: React.FC = () => {
  return (
    <div className="w-full h-full bg-gradient-to-br from-blue-200 via-pink-300 to-blue-800">
      {/* This component is just the colorful background on the left side */}
    </div>
  );
};

export default AuthBackground;